# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Kalpataru Exhibition — business website, scaffolded 2026-08-17 with Astro 7 (minimal template). Multi-page V1 built and verified: light warm-paper theme, teal accent + palette matched to the user's logo (light ink `#016585` / `#0d9098`), homepage with 12 sections plus Work, Services, Industries, About, Contact, Privacy Policy and Terms & Conditions pages. GSAP + ScrollTrigger motion with prefers-reduced-motion support. Committed and pushed to GitHub: `origin` = https://github.com/nomeshexponentiagit/kalpataru.git (branch `main`).

**Hero media:** the hero plays a 12s seamless Ken Burns loop (`public/videos/hero.mp4`, h264) made from a real stall photo, with `public/images/hero.jpg` as poster/first frame and a fade-in once playable. It is generated from `/tmp/kalpataru-verify/render-video-photo.html` (headless-Chrome MediaRecorder) and re-encoded with ffmpeg-static. Title readability is enforced by a left/top scrim in the video itself plus `.hero__scrim` in `Hero.astro` — before lightening either, re-measure with `/tmp/kalpataru-verify/line-contrast2.mjs` (WCAG large-text 3:1 target).

**Real assets (user-approved):** stall photos in `public/images/` (work-*, case-*, cta.jpg, about-hero.jpg, about-story.jpg) and all of `public/images/clients/` come from the user's live kalpataruexhibition.com portfolio ("What We Have Clients" + portfolio sections). `public/images/work/` (81 gallery photos) and `public/images/work-hero.jpg` are batch-resized from the same source with REAL client captions (see the Work page below); they are generated by `/tmp/kalpataru-verify/make-work-gallery.mjs` — re-run it after adding new photos, then rebuild. The remaining photos (services, industries) are DUMMY stock and must be replaced.

**Dummy-data policy (user-mandated):** the site still uses DUMMY stock photos and DUMMY content, marked with "DUMMY" comments in source. All dummy images live in `public/images/` (replace by overwriting the file, or update the `src` prop in the section component). Client names, stats, testimonials, case studies, and contact details (email/phone/address/hours) in `src/data/site.ts` are invented and must be replaced with real, verified information before launch. All legal text on the Privacy Policy and Terms pages is placeholder — have real legal text drafted before launch. The header auto-detects a logo file (`logo.svg/png/webp/jpg`) dropped into `public/brand/`; `public/brand/logo.png` (the real logo) currently exists.

## Clean project rule (user-mandated)

This is a completely new website, built from scratch. A rule set by the user:

- **Never** search for, open, inspect, or copy from any older Kalpataru Exhibition website/project that may exist elsewhere on this computer. If one is discovered, ignore it.
- Do not reuse CSS, JavaScript, PHP, HTML, design, architecture, or content from any previous project.
- Treat this project as completely blank.

The **only** sources of truth for this website are:
1. Instructions provided in this project
2. Information explicitly provided by the user
3. Approved assets provided for this project
4. Technical requirements documented in this CLAUDE.md

## Commands

- `npm run dev` — start the dev server at http://localhost:4321. For long-running work prefer `astro dev --background`; manage it with `astro dev stop`, `astro dev status`, `astro dev logs`.
- `npm run build` — production build to `dist/` (run before deploying)
- `npm run preview` — serve the built site locally
- `npx astro check` — typecheck the project (`@astrojs/check`, `typescript`, `@types/node` are installed)
- `npx astro add <integration>` — add an integration (e.g. `npx astro add tailwind`)
- **Caveat:** after editing `.astro` components while dev runs, restart the dev server before visual verification — Vite can serve stale scoped CSS. If `astro dev stop` reports no server while :4321 still responds, kill the PID from `lsof -nP -i :4321` and start fresh from the project root (a start from another cwd fails with "Missing pages directory").

## Architecture

**Approved stack (user-mandated):** Astro 7 (static site, SSG), HTML5, CSS3, vanilla JavaScript, GSAP for animations. PHP only if a backend is later required; MySQL only if a database is later required.

- **Do NOT add** React, Next.js, Tailwind, Bootstrap, or any other frontend framework unless explicitly instructed.
- **Do NOT use Stitch** (or other design-generation tools) for this project — design is done directly in this Astro codebase with Claude Code, based on user requirements.
- **File-based routing**: `src/pages/` — every `.astro` file is a route (`src/pages/index.astro` → `/`, plus `work.astro`, `services.astro`, `industries.astro`, `about.astro`, `contact.astro`, `privacy-policy.astro`, `terms-and-conditions.astro`).
- **Services page** (`src/pages/services.astro`) — the six brief titles as detailed rows (desc + "includes" chips are DUMMY copy) + a 5-step process strip + CTA. **Industries page** (`src/pages/industries.astro`) — the nine brief industries as cards (blurbs DUMMY), three design rules (DUMMY) and a REAL client-name strip computed from `src/data/work.ts` captions.
- **Work page** (`src/pages/work.astro`) — portfolio gallery from `src/data/work.ts` (81 REAL photos grouped Engineering / Real Estate / Jewellery, client captions copied verbatim from the live site; the sector blurbs in work.ts are DUMMY copy until the user approves). Tiles are buttons feeding a vanilla-JS lightbox (Esc/←/→/backdrop close, scroll lock, focus restore).
- `src/layouts/Layout.astro` — shared page shell (head, nav, footer); wrap every new page in it.
- `src/styles/global.css` — global styles, imported by the layout. Light theme by default (warm paper `--bg`, deep-teal ink `--ink`); photo-led sections (Hero, Industries, Final CTA) carry `.theme-dark` to keep light-on-dark ink. Section background tones: `.tone-white`, `.tone-tint`, `.tone-ink`, `.tone-accent`. `.prose` styles the legal pages' article text.
- `src/components/ui/PageHero.astro` — shared inner-page header (eyebrow + masked title lines + optional subtitle/photo). It carries the `data-hero-*` hooks, so `scripts/motion.ts` plays the standard hero entrance on every page that uses it; it also sets `id="top"` so footer/back-to-top anchors work. Legal pages omit the photo (teal glow backdrop) and use `compact`.
- `src/components/ui/PlaceholderImage.astro` — image wrapper: renders `<img>` when given `src`, else a neutral SVG motif. Handles aspect-ratio, tone, alt/aria.
- **Nav links are home-aware:** `NAV_LINKS`/`FOOTER_NAV` in `src/data/site.ts` store section links as `/#locations` etc.; `Header.astro` and `Footer.astro` compute `const isHome = Astro.url.pathname === '/'` and strip the leading slash via `navHref()` while on the homepage. Work/Services/Industries/About/Contact are real routes; legal links (`LEGAL_LINKS`) render in the footer bottom bar.
- **Contact form** (`src/pages/contact.astro`) is static-site only: on submit it opens the visitor's mail client addressed to `SITE.email` via a `mailto:` built inline (script uses `define:vars`, so it must stay plain JS — no TS annotations). Swap in a real backend handler when one exists.
- **Motion wiring:** `src/scripts/motion.ts` owns GSAP (hero entrance keyed off `[data-hero-media]`, scroll reveals on `[data-reveal]`, stat counters on `[data-stat]`); `main.ts` owns header scroll state, mobile menu, service/industry hover interactions. Both must tolerate missing elements without console warnings (inner pages reuse the homepage selectors).
- `public/` — static assets served at the root: `images/` (photos incl. real portfolio shots), `videos/` (hero.mp4), `brand/` (user logo), favicon, fonts. Add brand assets here.
- `astro.config.mjs` — Astro config; `tsconfig.json` — TypeScript config.
- Pure `.astro` components — no JS framework installed. Node >= 22.12 required.
